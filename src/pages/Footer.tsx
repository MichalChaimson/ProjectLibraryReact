import AppBar from '@mui/material/AppBar';

export default function DenseAppBar() {
  return (
    <AppBar position="fixed" sx={{ bottom: 0, height: '50px', top: 'unset', backgroundColor: 'blue' }} >
    </AppBar>
  );
}
